spring:
  profiles:
#    active: ollama
    active: openai
#    active: gemini
  application:
    name: springai-agentexecutor
  main:
# console app
    web-application-type: none
# studio
#     web-application-type: servlet

  ai:
    openai:
      base-url: https://api.novita.ai/openai
      api-key: sk_4gl2Op_cec21CRV2NEC0xImoXbhPTUQ6aY695T3HBHw
      chat:
        options:
          # Llama 3.3 70B Instruct - supports tool calling but may not support standard function calling schema
          # If this doesn't work, try Ollama profile with a local model
          model: meta-llama/llama-3.3-70b-instruct
    # ollama:
    #   base-url: https://api.novita.ai/openai
    #   chat:
    #     options:
    #       model: google/gemma-3-27b-it
    #       temperature: 0.3
weather:
  api-key: ${WEATHER_API_KEY}
  api-url: https://api.weatherapi.com/v1

tavily:
  api-key: ${TAVILY_API_KEY:}

# Source code search agent configuration
source:
  src_target: ${SRC_TARGET:src/main/java}
  doc_target: ${DOC_TARGET:docs}

